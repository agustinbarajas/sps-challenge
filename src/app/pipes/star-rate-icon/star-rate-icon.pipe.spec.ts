import { StarRateIconPipe } from './star-rate-icon.pipe';

describe('StarRateIconPipe', () => {
  it('create an instance', () => {
    const pipe = new StarRateIconPipe();
    expect(pipe).toBeTruthy();
  });
});
